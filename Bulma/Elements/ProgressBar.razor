@namespace easy_blazor_bulma
@inherits ComponentBase

@if (Current != null)
{
    <progress class="@FullCssClass" max="@Max" value="@Current">@Current</progress>
}
else
{
    <progress class="@FullCssClass" max="@Max">@Max</progress>
}
